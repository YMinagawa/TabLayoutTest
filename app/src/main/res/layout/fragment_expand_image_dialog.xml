<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="wrap_content"
                android:id="@+id/rl_expandimage"
             tools:context="jp.techacademy.yoshihiro.minagawa.tablayouttest.ui.tabui.analyzedata.ExpandImageDialogFragment">

    <!-- TODO: Update blank fragment layout -->

    <!-- タイトル -->
    <!--
    <TextView
        android:id = "@+id/textView_df_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_fdialog_title"
        android:padding="10dp"
        android:text="Title"
        android:textSize = "18dp"
        android:textColor = "@android:color/white"/>
    -->

    <!--android:layout_below = "@+id/textView_df_title"-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop = "true"
        android:layout_marginTop="6dp"
        android:layout_marginLeft="6dp"
        android:layout_marginRight="6dp"
        android:background="@drawable/bg_fdialog"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        >

        <jp.techacademy.yoshihiro.minagawa.tablayouttest.ui.CustomImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="fitCenter"
            android:id = "@+id/customImageView_expand"/>


        <!-- OK ボタン
<Button
    android:id="@+id/btn_df_positive"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_fdialog_positive"/>
 -->
    </LinearLayout>

    <Button
        android:id = "@+id/btn_close"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/bg_fdialog_close"
        android:text = "x"
        android:textColor="#9acd32"
        />

</RelativeLayout>
